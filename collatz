def collatz(number):
  if number%2==0:
    x = (number // 2)
  else:
    x = 3*number +1
  print (x)
  return (x)

print ("Enter a positive integer")
y = int(input ())
while y > 1:
    y = collatz (y)
